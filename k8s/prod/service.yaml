apiVersion: v1
kind: Service

metadata:
  name: discreta

spec:
  type: NodePort

  selector:
    app: discreta

  ports:
    - protocol: TCP
      port: 30000
      targetPort: 3000
      nodePort: 30000

