apiVersion: v1
kind: Service

metadata:
  name: discreta-staging-service

spec:
  selector:
    app: discreta-staging

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30000

  type: NodePort